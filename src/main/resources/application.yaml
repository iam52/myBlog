spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
jwt:
  issuer: 1234@gmail.com
  secret-key: ${JWT_SECRET_KEY}