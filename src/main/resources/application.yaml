spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: dummy
            client-secret: dummy
            scope:
              - email
              - profile
jwt:
  issuer: ajufresh@gmail.com
  secret-key: study-springboot